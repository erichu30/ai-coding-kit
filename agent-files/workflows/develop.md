# Develop

Use the `ai-coding-workflow` skill from
`.agents/skills/ai-coding-workflow/SKILL.md` for the requested repository change.

Treat the text following `/develop` as the requested change. Complete the workflow only
through a verified local commit, then stop for human review. Push or publish only after
separate explicit approval.
